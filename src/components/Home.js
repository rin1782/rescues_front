import React from 'react' 
import {Route, Link} from 'react-router-dom';

const Home = (props) => {
    return(
        <h1>
            The Underdog Rescue Network
        </h1>
    )
}

export default Home